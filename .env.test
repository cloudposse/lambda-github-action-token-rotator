# Placeholder fixture values. The original file committed the private key of GitHub App 217830
# (third-party "skedrocket" org, since deleted) — the sole handler test that consumed these values
# is skipped pending https://github.com/cloudposse/lambda-github-action-token-rotator/issues/63.
# When restoring live tests, inject real values via secrets — never commit them here.
AWS_REGION="us-east-1"
GITHUB_APP_ID="000000"
GITHUB_INSTALLATION_ID="00000000"
GITHUB_ORG="example-org"
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nplaceholder\n-----END RSA PRIVATE KEY-----\n"
